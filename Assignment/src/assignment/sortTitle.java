package assignment;

import java.util.Comparator;
public class sortTitle implements Comparator<tour>{
     public int compare(tour tour1, tour tour2){
        return tour1.getTitle().compareTo(tour1.getTitle());
    }
}
